/**
 *
 *
 * @export
 * @class Passport
 */
module.exports = class Passport {
    /**
     *Creates an instance of Passport.
     * @param {*} client
     * @memberof Passport
     */
    constructor(client) {
        this.client = client;
    }
};
