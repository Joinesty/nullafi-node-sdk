/**
 *
 *
 * @export
 * @class Gender
 */
module.exports = class Gender {
    /**
     *Creates an instance of Gender.
     * @param {*} client
     * @memberof Gender
     */
    constructor(client) {
        this.client = client;
    }
};
